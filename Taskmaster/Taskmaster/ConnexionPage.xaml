<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Taskmaster.Views.ConnexionPage"
             Title="Connexion">

	<VerticalStackLayout Padding="20" Spacing="15">

		<Label Text="Connexion" FontSize="24" HorizontalOptions="Center" />

		<Entry x:Name="EmailEntry" Placeholder="Email" Keyboard="Email" />
		<Entry x:Name="MotDePasseEntry" Placeholder="Mot de passe" IsPassword="True" />

		<Button Text="Se connecter" Clicked="OnConnexionClicked" />

		<Button Text="Pas de compte ? Créez-en un"
                Clicked="OnInscriptionRedirectClicked"
                TextColor="Blue"
                BackgroundColor="Transparent" />
	</VerticalStackLayout>
</ContentPage>
